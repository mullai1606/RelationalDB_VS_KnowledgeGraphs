initialize the db with these sql queries 

CREATE TABLE role (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL
);

CREATE TABLE user_role (
    user_id INT NOT NULL,
    role_id INT NOT NULL,
    PRIMARY KEY (user_id, role_id),
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE,
    FOREIGN KEY (role_id) REFERENCES role(id) ON DELETE CASCADE
);

INSERT INTO roles (name) VALUES 
('admin'), ('consumer'), ('supplier'), ('manager'), ('developer'),
('tester'), ('support_agent'), ('sales'), ('marketing'), ('analyst'),
('hr'), ('designer'), ('product_owner'), ('scrum_master'), ('intern'),
('guest'), ('editor'), ('publisher'), ('auditor'), ('moderator');
